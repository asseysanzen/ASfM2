<% if user_signed_in? %>
	<% if current_user != user %>
        <% if current_user.following?(user) %>
            <%= link_to 'Unfollow', users_user_relationships_path(user.id), method: :delete, class:"btn btn-default" %>
        <% else %>
              <%= link_to 'follow', users_user_relationships_path(user.id), method: :POST, class:"btn btn-default" %>
        <% end %>
    <% end %>
<% end %>