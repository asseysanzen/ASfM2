<div class="headline">
	<h3 class="headline_text">ジャンル</h3>
</div>

<div class="genre_form">
	<%= form_for @genre_new, url: admins_genres_path do |f| %>
		<%= f.text_field :kind %>
		<%= f.select :status, Genre.statuses.keys.to_a %>
  		<%= f.submit '追加' %>
  	<% end %>
</div>
<div class="genrelists">
	<table class="table table-hover table-inverse">
		<thead>
			<tr>
				<th>ジャンル</th>
				<th>状態</th>
				<th></th>
			</tr>
		</thead>

		<tbody>
			<% @genres.each do |genre| %>
			<tr>
				<td>
					<%= link_to admins_genre_path(genre.id) do %>
						<%= genre.kind %>
					<% end %>
				</td>
				<td><%= genre.status %></td>
				<td><%= link_to "編集", edit_admins_genre_path(genre.id), class:"btn btn-primary" %></td>
			</tr>
			<% end %>
		</tbody>
	</table>
</div>