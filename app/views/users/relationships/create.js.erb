if (location.pathname == '/users/users/<%= @user.id %>' ) {
	$(".user_<%= @user.id %>").html("<%= j(render 'partials/user',  user: @user ) %>");
} else {
	$(".user_<%= @user.id %>").html("<%= j(render 'partials/users_content',  user: @user ) %>");
}
