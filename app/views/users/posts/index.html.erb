<h2>Posts</h2>
  <% @posts.each do |post| %>
    <div>
      <div>
        <%= link_to users_post_path(post.id) do %>
          <%= attachment_image_tag post, :image, size:"150x150" %>
        <% end %>
      </div>
      <div>
        <%= post.price %>円
      </div>
    </div>
  <% end %>