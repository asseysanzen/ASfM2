<h2>お気に入り</h2>

<%= render 'partials/sidebar', genres: @genres %>

<% if @posts.count > 0 %>
	<div>
    	<%= render 'partials/posts', posts: @posts %>
	</div>
<% else %>
	<div>
		<p>
			いいねしたアイテムがありません。<br>
			気になるアイテムがある場合いいねすることをおすすめします。
		</p>
	</div>
<% end %>